## Proposed Changes

- Redirect `/privacy-statement/` → `/privacy-policy`
- Redirect `/services/` → `/our-services`
- Redirect `/services/servicing/` → `/services/car-servicing`
- Redirect `/services/mots/` → `/services/mot-testing`
- Redirect `/services/other-services/` → `/our-services`
- Redirect `/book-online/` → `/contact`

All redirects will be permanent (301), so any SEO value from the old URLs passes through to the correct pages. Trailing slashes are handled automatically.

**Estimated cost: £1.30 – £2.50**

Since this is a small, tidy change, it'd be great value to bundle anything else you've been meaning to update at the same time — anything else you'd like sorted while we're at it?