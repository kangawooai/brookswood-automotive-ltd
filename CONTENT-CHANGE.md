## Proposed Changes

- Download and host all 10 UniNeue font files locally in the project
- Remove Rubik and Inter from Google Fonts
- Set up UniNeue as the single font family across the entire site with the following mapping:
  - **UniNeue Black Italic** → headings (h1–h6)
  - **UniNeue Regular Italic** → body text and general copy
  - **UniNeue Bold Italic** → buttons, navigation, labels, badges
  - **UniNeue Heavy (upright)** → heavy emphasis elements
- Update the global CSS and font configuration to use UniNeue throughout
- Remove all references to `--font-inter` and `--font-rubik` variables

**Estimated cost: £3 – £6**