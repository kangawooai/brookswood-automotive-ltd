## Proposed Changes

- **Fix the contrast issue** in the CTA band — change `text-primary-foreground/85` to `text-primary-foreground/90` to meet WCAG AA
- **Improve hero image compression** — increase compression on the hero workshop image to reduce its size (~7–12 KiB saving)
- **Add a `preconnect` hint** for `www.googletagmanager.com` — Lighthouse identified this as saving ~210ms on LCP

**Estimated cost: £1.50 – £3.50**

Since this is a small set of tweaks, you'll get better value bundling them together — is there anything else you'd like sorted at the same time, such as any copy changes, design tweaks, or other improvements?