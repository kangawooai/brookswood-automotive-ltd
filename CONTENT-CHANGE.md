## Proposed Changes

- Update the central site config (`SITE` data) so the new number flows automatically to every page and component that references it
- Update the display number from **01329 640779** → **01329 640528** sitewide
- Update the click-to-call `tel:` link to match (e.g. `tel:01329640528`) sitewide
- This covers: the header, footer, contact page, MOT landing page, CTA bands, hero sections, the thank-you page, and anywhere else the number appears — including any hardcoded instances in individual page files (e.g. the error message fallback in the contact form)

**Estimated cost: £1.40 – £3.65**